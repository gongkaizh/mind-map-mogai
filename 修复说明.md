# 思维导图项目问题修复说明

## 修复的问题

### 1. AI建议和AI解释功能无法正常使用

**问题原因：**
- AI配置中的 `key` 和 `model` 字段为空
- AI服务调用方式存在问题
- 事件处理机制不完善

**修复内容：**
1. **更新AI配置默认值** (`web/src/store.js`)
   - 设置了默认的API Key占位符
   - 设置了默认的模型名称

2. **重构AI建议功能** (`web/src/pages/Edit/components/AiSuggest.vue`)
   - 添加了直接的AI服务调用方法
   - 改进了错误处理和用户提示
   - 支持流式响应处理

3. **重构AI解释功能** (`web/src/service/aiService.js`)
   - 修改为通过本地AI服务代理调用
   - 添加了连接检测
   - 改进了错误处理

4. **添加国际化文本** (`web/src/lang/zh_cn.js`)
   - 补充了缺失的AI相关翻译文本

### 2. 导出.mm、.emmx、.mmap格式无法弹出文件保存选择框

**问题原因：**
- 导出功能使用直接下载方式，没有文件保存对话框
- 缺少对应格式的导出方法映射
- 浏览器安全限制

**修复内容：**
1. **改进文件下载机制** (`simple-mind-map/src/plugins/Export.js`)
   - 优先使用现代浏览器的 `showSaveFilePicker` API
   - 支持文件保存对话框
   - 保留传统下载方式作为回退方案
   - 添加了完整的文件类型映射

2. **添加导出格式方法**
   - 添加了 `mm()` 方法用于FreeMind格式导出
   - 添加了 `emmx()` 方法用于EdrawMind格式导出
   - 添加了 `mmap()` 方法用于MindManager格式导出

## 使用说明

### AI功能配置

1. **配置AI服务**
   - 点击右上角的AI配置按钮
   - 输入有效的API Key（替换默认的占位符）
   - 确认模型名称正确
   - 确保本地AI服务（端口3456）正在运行

2. **使用AI建议**
   - 右键点击任意节点
   - 选择"AI建议"
   - 等待AI生成建议内容
   - 点击"应用建议"将内容添加为子节点

3. **使用AI解释**
   - 右键点击任意节点
   - 选择"AI解释"
   - 查看AI生成的解释内容
   - 点击"应用"将解释添加为子节点

### 导出功能使用

1. **导出思维导图**
   - 点击工具栏的"导出"按钮
   - 选择所需的导出格式（.mm、.emmx、.mmap等）
   - 输入文件名
   - 点击"确定"

2. **文件保存**
   - 在支持的浏览器中会弹出文件保存对话框
   - 选择保存位置和文件名
   - 点击"保存"完成导出

## 技术细节

### AI服务架构
- 前端通过本地代理服务（端口3456）调用AI API
- 支持流式响应处理
- 包含连接检测和错误处理机制

### 导出功能架构
- 优先使用File System Access API（现代浏览器）
- 回退到传统的blob下载方式
- 支持多种思维导图格式

### 文件结构
```
web/
├── src/
│   ├── components/
│   │   ├── AiSuggest.vue      # AI建议组件
│   │   └── AiExplain.vue      # AI解释组件
│   ├── service/
│   │   └── aiService.js       # AI服务调用
│   ├── store.js               # 状态管理
│   └── lang/
│       └── zh_cn.js           # 中文翻译
└── scripts/
    └── ai.js                  # AI代理服务

simple-mind-map/
└── src/
    └── plugins/
        └── Export.js          # 导出插件
```

## 注意事项

1. **AI功能需要有效的API配置**
   - 确保API Key有效且有足够的配额
   - 确保网络连接正常
   - 确保本地AI服务正在运行

2. **导出功能兼容性**
   - 文件保存对话框需要现代浏览器支持
   - 在不支持的浏览器中会自动回退到直接下载

3. **开发环境**
   - 使用 `npm run dev` 启动开发服务器
   - AI服务会自动在端口3456启动
   - 前端服务在端口8081启动

## 测试建议

1. **测试AI功能**
   - 配置有效的API Key
   - 测试AI建议和AI解释功能
   - 验证错误处理机制

2. **测试导出功能**
   - 测试各种导出格式
   - 验证文件保存对话框
   - 测试在不同浏览器中的兼容性
