# 思维导图项目 - 启动说明

## 快速启动

### 可用启动脚本（均为稳定英文版）

#### 1. 完整启动（Web + Electron）⭐ 最常用
**双击运行：[start-all.bat](start-all.bat)**

功能：
- ✅ 自动检查并安装依赖
- ✅ 启动 Web 开发服务器（http://localhost:8080）
- ✅ 启动 Electron 桌面应用
- ✅ 两个服务在独立窗口运行

#### 2. 快速启动（Web + AI 服务）
**双击运行：[快速启动.bat](快速启动.bat)**

功能：
- ✅ 仅启动 Web 开发服务器 + AI 服务
- ✅ 单窗口运行
- ✅ 适合纯前端开发

#### 3. 选择启动模式
**双击运行：[启动项目.bat](启动项目.bat)**

功能：
- ✅ 提供 3 个选项：
  1. 仅 Web 服务器（端口 8080）
  2. Web + AI 服务（端口 8080 + 3456）
  3. Electron 桌面应用

#### 4. 仅启动桌面应用
**双击运行：[启动桌面应用.bat](启动桌面应用.bat)**

功能：
- ✅ 仅启动 Electron 桌面应用
- ✅ 单窗口运行

---

## 启动后

### start-all.bat（完整启动）
你会看到三个窗口：
1. **启动提示窗口** - 显示启动状态，可按任意键关闭
2. **Web 服务器窗口** - 运行在端口 8080
3. **Electron 应用窗口** - 桌面应用界面

### 其他脚本
会在当前窗口运行服务，按 `Ctrl+C` 可停止。

---

## 停止服务

关闭对应的窗口即可停止服务：
- 关闭 Web 服务器窗口 → 停止 Web 服务
- 关闭 Electron 窗口 → 停止桌面应用

---

## 其他启动方式

### 仅启动 Web 服务器
```bash
cd web
npm run serve
```

### 仅启动 Electron 应用
```bash
npm run electron
```

---

## 启动脚本对比

| 脚本 | Web服务器 | AI服务 | Electron | 运行方式 | 适用场景 |
|------|----------|--------|----------|---------|---------|
| **start-all.bat** | ✅ 8080 | ✅ | ✅ | 多窗口独立运行 | 完整开发  |
| **快速启动.bat** | ✅ 8080 | ✅ | ❌ | 单窗口运行 | 前端开发 |
| **启动项目.bat** | 可选 | 可选 | 可选 | 单窗口运行 | 灵活选择 |
| **启动桌面应用.bat** | ✅ 8081 | ✅ | ✅ | 单窗口运行 | 桌面测试 |

---

## 注意事项

- 所有启动脚本均为**英文界面**，稳定可靠
- 首次运行会自动安装依赖，需要 1-3 分钟
- 确保已安装 Node.js（运行 `node -v` 检查）
- 如果端口被占用，请关闭占用端口的程序

---

**启动脚本位置：项目根目录**
**所有脚本均为英文版本，功能完整稳定**
