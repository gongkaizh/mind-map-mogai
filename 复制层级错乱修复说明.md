# 复制层级错乱修复说明

## 问题描述

当在根节点按 `Ctrl+C` 复制，然后粘贴到 XMind 时，出现节点层级错乱的问题：
- 原本应该在 XMind 中是 3 级节点的，其中一部分保持原样
- 但另一部分被错误地识别为 2 级节点

## 问题根因分析

通过代码分析发现问题出现在以下几个环节：

### 1. `getTopAncestorsFomNodeList` 函数问题
- **位置**: `simple-mind-map/src/utils/index.js:1078-1088`
- **问题**: 该函数在过滤顶层祖先节点时，可能改变了节点的原始顺序
- **影响**: 破坏了节点在思维导图中的层级关系

### 2. `sortNodeList` 函数局限性
- **位置**: `simple-mind-map/src/utils/index.js:1986-1988`
- **问题**: 仅根据 `sortIndex` 排序，无法正确反映复杂的层级关系
- **影响**: 排序后的节点顺序与原始层级结构不符

### 3. 复制数据格式不完整
- **位置**: `simple-mind-map/src/core/render/Render.js:2088-2108`
- **问题**: 复制的数据缺少明确的层级信息
- **影响**: XMind 无法正确识别节点的层级关系

## 修复方案

### 1. 改进 `getTopAncestorsFomNodeList` 函数

```javascript
// 修复前：可能改变节点顺序
export const getTopAncestorsFomNodeList = list => {
  let res = []
  list.forEach(node => {
    if (!list.find(item => {
      return item.uid !== node.uid && item.isAncestor(node)
    })) {
      res.push(node)
    }
  })
  return res
}

// 修复后：保持原始顺序
export const getTopAncestorsFomNodeList = list => {
  let res = []
  list.forEach(node => {
    if (!list.find(item => {
      return item.uid !== node.uid && item.isAncestor(node)
    })) {
      res.push(node)
    }
  })
  
  // 按照在原始列表中的顺序排序，保持层级关系
  res.sort((a, b) => {
    const indexA = list.findIndex(item => item.uid === a.uid)
    const indexB = list.findIndex(item => item.uid === b.uid)
    return indexA - indexB
  })
  
  return res
}
```

### 2. 新增层级感知的排序方法

在 `Render.js` 中新增 `sortNodeListByHierarchy` 方法：

```javascript
// 按层级关系排序节点列表，确保复制到XMind时层级正确
sortNodeListByHierarchy(nodeList) {
  const nodesWithHierarchy = nodeList.map(node => {
    return {
      node: node,
      layerIndex: node.layerIndex || 0,
      sortIndex: node.sortIndex || 0,
      indexInParent: node.parent ? node.getIndexInBrothers() : 0,
      hierarchyPath: this.getNodeHierarchyPath(node)
    }
  })

  // 按层级路径排序，确保保持正确的层级关系
  nodesWithHierarchy.sort((a, b) => {
    // 首先按层级深度排序
    if (a.layerIndex !== b.layerIndex) {
      return a.layerIndex - b.layerIndex
    }
    
    // 同层级的节点按层级路径排序
    const pathA = a.hierarchyPath
    const pathB = b.hierarchyPath
    
    for (let i = 0; i < Math.min(pathA.length, pathB.length); i++) {
      if (pathA[i] !== pathB[i]) {
        return pathA[i] - pathB[i]
      }
    }
    
    return a.sortIndex - b.sortIndex
  })

  return nodesWithHierarchy.map(item => item.node)
}
```

### 3. 增强 XMind 兼容性

新增 `enhanceDataForXMindCompatibility` 方法：

```javascript
// 为XMind兼容性增强数据格式
enhanceDataForXMindCompatibility(data) {
  const enhanceNode = (node, level = 0) => {
    const enhanced = { ...node }
    
    // 添加层级标识
    if (!enhanced.data) enhanced.data = {}
    enhanced.data._level = level
    enhanced.data._isTopLevel = level === 0
    
    // 递归处理子节点
    if (enhanced.children && enhanced.children.length > 0) {
      enhanced.children = enhanced.children.map(child => 
        enhanceNode(child, level + 1)
      )
    }
    
    return enhanced
  }
  
  return data.map(node => enhanceNode(node))
}
```

## 修复效果

### 修复前的问题
1. 节点复制时顺序可能被打乱
2. 层级关系在排序过程中丢失
3. XMind 无法正确识别节点层级

### 修复后的改进
1. ✅ 保持节点的原始顺序和层级关系
2. ✅ 使用层级路径进行智能排序
3. ✅ 为复制数据添加明确的层级标识
4. ✅ 提高与 XMind 的兼容性

## 测试验证

创建了测试页面 `test-copy-hierarchy-fix.html` 来验证修复效果：

1. **测试数据**: 包含多层级节点的复杂结构
2. **功能测试**: 验证复制功能的层级处理
3. **兼容性测试**: 检查 XMind 兼容性增强效果

## 使用说明

1. 修复已自动应用到复制功能中
2. 无需额外配置，保持原有使用方式
3. 复制到 XMind 时层级关系将保持正确

## 注意事项

1. 修复主要针对复制到 XMind 的场景
2. 不影响应用内部的粘贴功能
3. 保持向后兼容性，不破坏现有功能

## 相关文件

- `simple-mind-map/src/core/render/Render.js` - 主要修复逻辑
- `simple-mind-map/src/utils/index.js` - 工具函数改进
- `test-copy-hierarchy-fix.html` - 测试验证页面