# 批量导出功能说明

## 功能概述

新增的批量导出功能允许用户同时导出多种格式的思维导图文件，大大提高了导出效率。用户可以一次性选择多种格式（如.mm、.emmx、.mmap、.png、.pdf等），系统会自动逐个导出并下载。

## 功能特点

### 1. 多格式同时导出
- 支持所有现有的导出格式
- 可以任意组合选择需要的格式
- 一次操作完成多个文件的导出

### 2. 智能文件命名
- 自动为每种格式添加正确的文件扩展名
- 基于用户输入的基础文件名生成
- 避免文件名冲突

### 3. 进度反馈
- 实时显示导出进度
- 成功/失败状态提示
- 详细的错误信息记录

### 4. 浏览器兼容
- 自动处理浏览器下载限制
- 在文件间添加适当延迟避免被阻止
- 支持现代浏览器的文件保存对话框

## 使用方法

### 1. 开启批量导出模式
1. 点击"导出"按钮打开导出对话框
2. 在左侧格式列表顶部勾选"批量导出"选项
3. 界面会切换到批量选择模式

### 2. 选择导出格式
1. 在格式列表中点击或勾选需要的格式
2. 可以选择多种格式的任意组合
3. 选中的格式会有蓝色高亮显示

### 3. 设置文件名
1. 在"导出文件名称"输入框中输入基础文件名
2. 系统会自动为每种格式添加对应的扩展名
3. 例如：输入"我的思维导图"，会生成"我的思维导图.mm"、"我的思维导图.png"等

### 4. 执行导出
1. 点击"导出"按钮开始批量导出
2. 系统会显示加载状态
3. 逐个下载每种格式的文件

## 支持的格式

### 文档格式
- **.smm** - SimpleMindMap专有格式
- **.json** - JSON数据格式
- **.md** - Markdown文档
- **.txt** - 纯文本文件

### 思维导图软件格式
- **.mm** - FreeMind格式
- **.emmx** - EdrawMind格式
- **.mmap** - MindManager格式
- **.xmind** - XMind格式

### 图像和文档格式
- **.png** - PNG图片
- **.svg** - SVG矢量图
- **.pdf** - PDF文档

## 技术实现

### 1. 前端界面改进
- 在导出对话框中添加批量导出选项
- 支持多选的格式列表界面
- 动态切换单选/多选模式

### 2. 批量导出逻辑
```javascript
// 批量导出核心逻辑
async batchExport(types, baseName) {
  for (const type of types) {
    try {
      const fileName = `${baseName}.${type}`
      await this.mindMap.export(type, true, fileName)
      // 添加延迟避免浏览器阻止
      await new Promise(resolve => setTimeout(resolve, 500))
    } catch (error) {
      // 错误处理
    }
  }
}
```

### 3. 错误处理
- 单个格式导出失败不影响其他格式
- 详细的错误日志记录
- 用户友好的错误提示

### 4. 国际化支持
- 中文、英文、繁体中文、越南语支持
- 动态的成功/失败消息
- 本地化的界面文本

## 使用场景

### 1. 多平台分享
需要在不同平台分享思维导图时，可以同时导出：
- PNG图片用于社交媒体分享
- PDF文档用于正式文档
- FreeMind格式用于技术团队

### 2. 备份存档
创建完整的思维导图备份：
- SMM格式保留完整数据
- PNG格式作为可视化备份
- Markdown格式便于文本检索

### 3. 跨软件协作
与使用不同思维导图软件的团队协作：
- 同时导出MM、EMMX、MMAP格式
- 确保所有团队成员都能打开文件

### 4. 演示准备
准备演示材料时一次性导出：
- PDF格式用于投影
- PNG格式用于插入PPT
- SVG格式用于网页展示

## 注意事项

### 1. 浏览器限制
- 某些浏览器可能限制同时下载多个文件
- 系统会自动在下载间添加延迟
- 建议允许浏览器的多文件下载

### 2. 文件大小
- 大型思维导图可能生成较大的文件
- 图片格式（PNG、PDF）文件通常较大
- 建议先测试小型图表

### 3. 格式兼容性
- 不同格式的功能支持程度不同
- 某些高级功能可能在简单格式中丢失
- 建议保留SMM格式作为主要备份

### 4. 性能考虑
- 批量导出需要更多时间
- 复杂图表的图片导出较慢
- 建议在网络稳定时进行批量导出

## 故障排除

### 1. 部分格式导出失败
- 检查浏览器控制台的错误信息
- 尝试单独导出失败的格式
- 确认思维导图内容没有特殊字符

### 2. 浏览器阻止下载
- 检查浏览器的下载设置
- 允许网站的多文件下载
- 尝试手动点击下载链接

### 3. 文件无法打开
- 确认目标软件版本兼容性
- 检查文件编码设置
- 尝试用文本编辑器查看文件内容

## 更新日志

### v1.0.0 (当前版本)
- ✅ 添加批量导出功能
- ✅ 支持所有现有导出格式
- ✅ 多语言界面支持
- ✅ 智能错误处理
- ✅ 浏览器兼容性优化

### 未来计划
- 📋 导出进度条显示
- 📋 自定义导出配置保存
- 📋 导出模板功能
- 📋 云端批量导出

## 总结

批量导出功能大大提升了SimpleMindMap的实用性，特别适合需要多格式输出的专业用户。通过简单的界面操作，用户可以快速获得多种格式的思维导图文件，满足不同场景的使用需求。

该功能的设计充分考虑了用户体验和技术稳定性，提供了完善的错误处理和国际化支持，是SimpleMindMap功能完善的重要一步。
