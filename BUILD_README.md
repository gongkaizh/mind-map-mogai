# 思维导图桌面应用 EXE 构建指南

## 🎯 概述

本项目提供了多种方式来构建思维导图桌面应用的Windows exe文件。

## 🚀 快速启动

### 可用启动脚本（均为英文版本）

项目提供多个启动脚本，让你可以快速开始开发：

#### 1. 完整启动 ⭐ 推荐
- **启动脚本**：`start-all.bat`
- **功能**：同时启动 Web 服务器和 Electron 应用
- **特点**：
  - 自动安装依赖
  - 启动 Web 开发服务器（端口 8080）
  - 启动 Electron 桌面应用
  - 两个服务在独立窗口运行

#### 2. 快速启动（Web + AI）
- **启动脚本**：`快速启动.bat`
- **功能**：仅启动 Web 开发服务器 + AI 服务
- **特点**：
  - 单窗口运行
  - 适合纯前端开发

#### 3. 选择启动模式
- **启动脚本**：`启动项目.bat`
- **功能**：提供 3 个选项
- **选项**：
  1. 仅 Web 服务器
  2. Web + AI 服务
  3. Electron 桌面应用

#### 4. 仅启动桌面应用
- **启动脚本**：`启动桌面应用.bat`
- **功能**：仅启动 Electron 桌面应用
- **特点**：单窗口运行

### 启动脚本对比

| 启动脚本 | Web服务器 | AI服务 | Electron | 运行方式 | 适用场景 |
|---------|----------|--------|---------|---------|---------|
| start-all.bat ⭐ | ✅ 8080 | ✅ | ✅ | 多窗口独立 | 完整开发 |
| 快速启动.bat | ✅ 8080 | ✅ | ❌ | 单窗口 | 前端开发 |
| 启动项目.bat | 可选 | 可选 | 可选 | 单窗口 | 灵活选择 |
| 启动桌面应用.bat | ✅ 8081 | ✅ | ✅ | 单窗口 | 桌面测试 |

**注意**：所有启动脚本均为英文版本，稳定可靠。中文版本由于 Windows 批处理文件编码问题已弃用。

## 📁 生成的文件位置

### 最新构建 (推荐)
- **位置**: `mind-map-exe-output/mind-map-desktop-202507311000-win32-x64/`
- **exe文件**: `mind-map-desktop-202507311000.exe`
- **特点**: 独立运行，包含所有依赖

### 其他构建输出
- `electron-dist/` - electron-builder构建输出
- `mind-map-exe-output/` - 自定义构建脚本输出

## 🚀 构建方式

### 方式1: 一键构建 (推荐)

#### Windows批处理文件
```bash
# 双击运行或在命令行执行
build-exe.bat
```

#### PowerShell脚本
```powershell
# 在PowerShell中执行
.\build-exe.ps1
```

### 方式2: 命令行构建

#### 快速构建
```bash
node quick-build-exe.js
```

#### 完整构建
```bash
node build-standalone-exe.js
```

#### 使用npm脚本
```bash
# 构建Windows版本
npm run pack-win

# 构建便携版
npm run pack-win-portable

# 构建所有平台
npm run pack-all
```

## 📦 构建选项说明

### 1. 快速构建
- 使用electron-builder
- 构建速度快
- 输出到`electron-dist/`目录
- 生成安装包和便携版

### 2. 完整构建
- 使用自定义脚本
- 包含完整的依赖复制
- 输出到`mind-map-exe-output/`目录
- 生成独立的应用程序目录

### 3. 仅构建web应用
- 只构建Vue.js前端
- 输出到`dist/`目录
- 用于更新web界面

## 🔧 构建要求

### 系统要求
- Windows 10/11
- Node.js 14+
- npm 6+

### 依赖安装
```bash
# 安装主项目依赖
npm install

# 安装web项目依赖
cd web
npm install
cd ..
```

## 📋 构建输出说明

### 文件结构
```
mind-map-exe-output/
└── mind-map-desktop-[时间戳]-win32-x64/
    ├── mind-map-desktop-[时间戳].exe  # 主程序
    ├── resources/                      # 应用资源
    ├── locales/                       # 语言文件
    └── [其他Electron运行时文件]
```

### 文件大小
- 主程序: ~150-200MB
- 完整目录: ~300-400MB

## 🎮 运行应用

1. 导航到构建输出目录
2. 双击exe文件启动应用
3. 应用将自动启动本地服务并打开界面

## 🐛 常见问题

### 构建失败
1. 检查Node.js版本是否兼容
2. 清理node_modules并重新安装
3. 检查网络连接（下载Electron二进制文件）

### 运行时错误
1. 确保所有依赖文件都在同一目录
2. 检查防火墙设置
3. 以管理员权限运行

### 文件过大
- 使用便携版构建: `npm run pack-win-portable`
- 考虑使用在线版本而非桌面版

## 📝 版本信息

- **应用版本**: 0.15.0
- **Electron版本**: 22.3.27
- **构建工具**: electron-builder 26.0.12

## 🔄 更新构建

要更新应用并重新构建:

1. 修改源代码
2. 运行构建脚本
3. 新的exe文件将包含时间戳以区分版本

## 📞 技术支持

如果遇到构建问题，请检查:
1. 控制台错误信息
2. 构建日志
3. 系统兼容性

---

**提示**: 推荐使用最新生成的exe文件 `mind-map-desktop-202507311000.exe`，它包含了最新的功能和修复。
