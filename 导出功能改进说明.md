# 导出功能改进说明

## 改进概述

基于用户反馈，我们对SimpleMindMap的导出功能进行了两项重要改进：

1. **智能文件命名**：导出文件名自动使用思维导图根节点内容
2. **ZIP打包批量导出**：批量导出时只弹出一个文件保存窗口，所有格式打包为ZIP文件

## 改进详情

### 1. 智能文件命名

#### 功能描述
- 打开导出对话框时，文件名自动填充为思维导图的根节点内容
- 自动清理文件名中的非法字符
- 如果根节点为空或获取失败，则使用默认文件名

#### 技术实现
```javascript
// 设置默认文件名为根节点内容
setDefaultFileName() {
  try {
    // 获取根节点内容
    const rootNodeText = this.$bus.mindMap?.renderer?.root?.nodeData?.data?.text
    if (rootNodeText && rootNodeText.trim()) {
      // 清理文件名中的非法字符
      this.fileName = this.sanitizeFileName(rootNodeText.trim())
    } else {
      this.fileName = this.$t('export.defaultFileName')
    }
  } catch (error) {
    console.warn('获取根节点内容失败:', error)
    this.fileName = this.$t('export.defaultFileName')
  }
}

// 清理文件名中的非法字符
sanitizeFileName(name) {
  return name
    .replace(/[<>:"/\\|?*]/g, '') // 移除Windows非法字符
    .replace(/\s+/g, ' ') // 合并多个空格
    .trim()
    .substring(0, 100) // 限制长度
}
```

#### 用户体验改进
- **自动化**：无需手动输入文件名
- **智能化**：文件名与思维导图内容相关
- **安全性**：自动处理非法字符，避免保存失败

### 2. ZIP打包批量导出

#### 功能描述
- 批量导出时，所有选中格式的文件打包为一个ZIP文件
- 只弹出一个文件保存对话框
- ZIP文件名格式：`{根节点内容}_批量导出.zip`

#### 技术实现
```javascript
// 批量导出核心逻辑
async batchExport(types, baseName) {
  // 动态导入JSZip
  const JSZip = (await import('jszip')).default
  const zip = new JSZip()

  // 逐个导出每种格式并添加到ZIP
  for (const type of types) {
    try {
      const fileName = `${baseName}.${type}`
      // 导出为Blob而不是下载
      const blob = await this.mindMap.export(type, false, fileName)
      
      // 添加到ZIP文件
      zip.file(fileName, blob)
      successCount++
    } catch (error) {
      // 错误处理
    }
  }

  // 生成并下载ZIP文件
  const zipBlob = await zip.generateAsync({ type: 'blob' })
  const link = document.createElement('a')
  link.href = URL.createObjectURL(zipBlob)
  link.download = `${baseName}_批量导出.zip`
  link.click()
}
```

#### 依赖管理
- 新增依赖：`jszip` - 用于创建ZIP文件
- 动态导入：减少初始包大小，按需加载

#### 用户体验改进
- **简化操作**：只需一次保存操作
- **文件整理**：所有格式整齐打包
- **避免阻止**：不会触发浏览器的多文件下载限制

## 使用示例

### 单个导出
1. 创建思维导图，根节点内容为"项目规划"
2. 点击"导出"按钮
3. 文件名自动填充为"项目规划"
4. 选择格式（如PNG）
5. 点击"导出"，下载"项目规划.png"

### 批量导出
1. 创建思维导图，根节点内容为"产品设计"
2. 点击"导出"按钮
3. 文件名自动填充为"产品设计"
4. 勾选"批量导出"
5. 选择多种格式（如PNG、PDF、FreeMind）
6. 点击"导出"，下载"产品设计_批量导出.zip"
7. ZIP文件包含：
   - 产品设计.png
   - 产品设计.pdf
   - 产品设计.mm

## 技术细节

### 文件名清理规则
- 移除Windows非法字符：`< > : " / \ | ? *`
- 合并多个连续空格为单个空格
- 去除首尾空格
- 限制长度为100个字符

### ZIP文件结构
```
产品设计_批量导出.zip
├── 产品设计.png
├── 产品设计.pdf
├── 产品设计.mm
├── 产品设计.emmx
└── 产品设计.mmap
```

### 错误处理
- 单个格式导出失败不影响其他格式
- 详细的错误日志记录
- 用户友好的成功/失败统计

### 性能优化
- JSZip动态导入，减少初始加载时间
- 异步处理，避免界面阻塞
- 内存管理，及时释放Blob URL

## 兼容性

### 浏览器支持
- Chrome 50+
- Firefox 45+
- Safari 10+
- Edge 12+

### 文件格式支持
- 所有现有导出格式均支持批量导出
- ZIP文件在所有主流操作系统上都能正常解压

## 国际化支持

### 更新的翻译文本
- **中文**：`同时导出多种格式的文件，打包为ZIP文件`
- **英文**：`Export multiple file formats simultaneously, packaged as ZIP file`
- **繁体中文**：`同時導出多種格式的檔案，打包為ZIP檔案`
- **越南语**：`Xuất đồng thời nhiều định dạng tệp, đóng gói thành tệp ZIP`

## 测试建议

### 功能测试
1. **智能命名测试**：
   - 创建不同根节点内容的思维导图
   - 验证文件名自动填充
   - 测试特殊字符处理

2. **批量导出测试**：
   - 选择不同格式组合
   - 验证ZIP文件生成
   - 检查ZIP内文件完整性

3. **边界情况测试**：
   - 空根节点内容
   - 超长根节点内容
   - 特殊字符根节点内容

### 性能测试
- 大型思维导图批量导出
- 多种格式同时导出的内存使用
- ZIP文件生成速度

## 总结

这两项改进显著提升了SimpleMindMap导出功能的用户体验：

1. **智能文件命名**让导出操作更加自动化和智能化
2. **ZIP打包批量导出**解决了多文件下载的操作复杂性

这些改进保持了向后兼容性，不影响现有的单个导出功能，同时为用户提供了更便捷的批量导出选项。
