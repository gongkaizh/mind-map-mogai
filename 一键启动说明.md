# 思维导图项目一键启动说明

## 📌 快速开始

项目现在提供了多个一键启动脚本，让你可以快速启动项目。

### 🚀 可用启动脚本（均为英文版本）

#### 1. 完整启动 ⭐ 推荐
**双击运行：** `start-all.bat`

- ✅ 自动安装依赖（如果未安装）
- ✅ 启动 Web 开发服务器（端口 8080）
- ✅ 启动 Electron 桌面应用
- ✅ 两个服务在独立窗口运行

#### 2. 快速启动（Web + AI 服务）
**双击运行：** `快速启动.bat`

- ✅ 启动 Web 开发服务器（端口 8080）
- ✅ 启动 AI 服务（端口 3456）
- ✅ 单窗口运行
- ✅ 适合纯前端开发

#### 3. 选择启动模式
**双击运行：** `启动项目.bat`

提供三个选项：
1. 仅启动 Web 开发服务器（端口 8080）
2. 启动 Web + AI 服务（端口 8080 + 3456）
3. 启动 Electron 桌面应用

#### 4. 仅启动桌面应用
**双击运行：** `启动桌面应用.bat`

- ✅ 仅启动 Electron 桌面应用
- ✅ 单窗口运行

## 📋 启动脚本对比

| 启动脚本 | Web服务器 | AI服务 | Electron | 运行方式 | 适用场景 |
|---------|----------|--------|---------|---------|---------|
| start-all.bat ⭐ | ✅ 8080 | ✅ | ✅ | 多窗口独立 | 完整开发 |
| 快速启动.bat | ✅ 8080 | ✅ | ❌ | 单窗口 | 前端开发 |
| 启动项目.bat | 可选 | 可选 | 可选 | 单窗口 | 灵活选择 |
| 启动桌面应用.bat | ✅ 8081 | ✅ | ✅ | 单窗口 | 桌面测试 |

## 🔧 手动启动命令

如果你想手动启动，可以使用以下命令：

### Web 开发模式
```bash
cd web
npm run serve          # 仅 Web 服务器
npm run dev            # Web + AI 服务
```

### Electron 桌面模式
```bash
npm run electron       # 生产模式
npm run electron-dev   # 开发模式
```

## 💡 常见问题

### 1. 脚本报错"找不到 npm"
确保你已经安装了 Node.js，可以在命令行运行 `node -v` 和 `npm -v` 检查。

### 2. 为什么是英文界面？
所有启动脚本均为英文版本，这是因为 Windows 批处理文件在处理中文编码时会出现乱码问题。英文版本稳定可靠，功能完全一致。

### 3. 端口被占用
如果提示端口被占用，可以：
- 关闭占用端口的程序
- 或者修改配置文件中的端口号

### 4. 依赖安装失败
尝试删除 `node_modules` 文件夹后重新运行启动脚本，它会自动重新安装依赖。

```bash
# 删除 web 目录的依赖
rm -rf web/node_modules

# 删除根目录的依赖
rm -rf node_modules

# 重新运行启动脚本
```

### 5. PowerShell 脚本
之前创建的 PowerShell 版本由于执行策略问题已弃用，现在统一使用 `.bat` 批处理文件。

## 🎨 开发建议

- **日常开发**：使用 `快速启动.bat`，支持热重载，修改代码后自动刷新
- **桌面应用测试**：使用 `启动桌面应用.bat`，测试完整的桌面应用功能
- **调试 AI 功能**：确保 AI 服务已启动（端口 3456）

## 📝 停止服务

在运行服务的终端窗口中按 `Ctrl+C` 即可停止服务。

---

**祝你开发愉快！** 🎉
