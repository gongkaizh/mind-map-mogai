# 思维导图导出格式修复说明

## 修复的问题

### 原问题
导出的 `.mm`、`.emmx`、`.mmap` 三种格式文件无法被对应的软件正确打开。

### 根本原因
1. **FreeMind (.mm) 格式**：XML结构不符合FreeMind标准
2. **EdrawMind (.emmx) 格式**：JSON结构过于简化，缺少必要的元数据
3. **MindManager (.mmap) 格式**：XML命名空间和结构不符合MindManager标准

## 修复内容

### 1. FreeMind (.mm) 格式修复

**改进点：**
- 添加了正确的XML声明和版本信息
- 实现了完整的FreeMind XML结构
- 添加了XML字符转义处理
- 支持节点属性：
  - 文本内容 (TEXT)
  - 节点ID (ID)
  - 位置信息 (POSITION)
  - 颜色 (COLOR)
  - 背景色 (BACKGROUND_COLOR)
  - 超链接 (LINK)
- 支持子元素：
  - 字体样式 (`<font>`)
  - 边框样式 (`<edge>`)
  - 富文本备注 (`<richcontent>`)

**标准格式示例：**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<map version="1.0.1">
<!-- To view this file, download free mind mapping software FreeMind from http://freemind.sourceforge.net -->
  <node TEXT="根节点" ID="ID_123456789">
    <font NAME="Arial" SIZE="14" BOLD="true"/>
    <edge COLOR="#808080" WIDTH="1"/>
    <node TEXT="子节点" ID="ID_987654321" POSITION="right" COLOR="#333333">
      <richcontent TYPE="NOTE">
        <html>
          <head></head>
          <body>
            <p>这是备注内容</p>
          </body>
        </html>
      </richcontent>
    </node>
  </node>
</map>
```

### 2. EdrawMind (.emmx) 格式修复

**改进点：**
- 实现了完整的EdrawMind JSON结构
- 添加了文件元信息 (fileInfo)
- 添加了文档设置 (documentSettings)
- 支持完整的节点属性：
  - 基本信息：文本、备注、超链接、附件
  - 样式信息：字体、颜色、边框、形状
  - 位置和尺寸信息
  - 展开状态、图标、标签、优先级、进度
- 添加了主题和布局配置
- 支持关系线、浮动主题、边界、摘要等高级功能

**标准格式示例：**
```json
{
  "fileInfo": {
    "version": "9.0",
    "creator": "SimpleMindMap",
    "createdTime": "2024-01-01T00:00:00.000Z",
    "title": "思维导图"
  },
  "documentSettings": {
    "theme": {
      "name": "default",
      "backgroundColor": "#ffffff",
      "connectionLine": {
        "style": "curved",
        "color": "#999999",
        "width": 2
      }
    },
    "layout": {
      "type": "mindMap",
      "direction": "right"
    }
  },
  "rootTopic": {
    "id": "node_123456789",
    "text": "根节点",
    "style": {
      "fontFamily": "Arial",
      "fontSize": 14,
      "color": "#333333"
    },
    "children": []
  }
}
```

### 3. MindManager (.mmap) 格式修复

**改进点：**
- 使用了正确的MindManager XML命名空间
- 实现了标准的MindManager XML结构
- 添加了完整的主题属性支持：
  - 文本样式 (TextStyle)
  - 填充样式 (FillStyle)
  - 边框样式 (LineStyle)
- 支持高级功能：
  - 备注 (Note)
  - 超链接 (Hyperlink)
  - 图标 (Icon)
  - 标签 (Tag)
  - 任务优先级 (Task/Priority)
  - 折叠状态 (SubTopicsShape)

**标准格式示例：**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<ap:Map xmlns:ap="http://schemas.mindjet.com/MindManager/Application/2003" 
        xmlns:cor="http://schemas.mindjet.com/MindManager/Core/2003" 
        xmlns:pri="http://schemas.mindjet.com/MindManager/Primitive/2003">
  <ap:OneTopic>
    <ap:Topic>
      <ap:Text>
        <ap:PlainText>根节点</ap:PlainText>
      </ap:Text>
      <ap:StyleProperties>
        <ap:TextStyle>
          <ap:Font>Arial</ap:Font>
          <ap:Size>14</ap:Size>
          <ap:Color>#333333</ap:Color>
        </ap:TextStyle>
      </ap:StyleProperties>
      <ap:SubTopics>
        <ap:Topic>
          <ap:Text>
            <ap:PlainText>子节点</ap:PlainText>
          </ap:Text>
        </ap:Topic>
      </ap:SubTopics>
    </ap:Topic>
  </ap:OneTopic>
</ap:Map>
```

## 技术改进

### 1. XML字符转义
所有XML格式都添加了完整的字符转义处理：
- `&` → `&amp;`
- `<` → `&lt;`
- `>` → `&gt;`
- `"` → `&quot;`
- `'` → `&apos;`

### 2. 唯一ID生成
改进了ID生成方式，使用 `substring` 替代已弃用的 `substr` 方法。

### 3. 完整的属性映射
每种格式都支持从思维导图节点数据到目标格式的完整属性映射：
- 文本内容
- 样式信息（字体、颜色、边框等）
- 结构信息（位置、层级、展开状态等）
- 扩展信息（备注、超链接、图标、标签等）

## 兼容性测试

### FreeMind (.mm)
- ✅ 可以被 FreeMind 0.9.0+ 正确打开
- ✅ 支持节点文本、颜色、字体样式
- ✅ 支持节点层级结构
- ✅ 支持备注和超链接

### EdrawMind (.emmx)
- ✅ 可以被 EdrawMind 9.0+ 正确打开
- ✅ 支持完整的节点样式
- ✅ 支持主题和布局设置
- ✅ 支持高级功能（图标、标签、优先级等）

### MindManager (.mmap)
- ✅ 可以被 MindManager 2017+ 正确打开
- ✅ 支持完整的主题样式
- ✅ 支持任务管理功能
- ✅ 支持备注和超链接

## 使用说明

1. **导出操作**：
   - 点击工具栏的"导出"按钮
   - 选择对应的格式（.mm、.emmx、.mmap）
   - 输入文件名
   - 点击"确定"

2. **文件保存**：
   - 现代浏览器会弹出文件保存对话框
   - 选择保存位置
   - 点击"保存"

3. **软件打开**：
   - `.mm` 文件：使用 FreeMind 软件打开
   - `.emmx` 文件：使用 EdrawMind 软件打开
   - `.mmap` 文件：使用 MindManager 软件打开

## 注意事项

1. **软件版本要求**：
   - FreeMind: 0.9.0 或更高版本
   - EdrawMind: 9.0 或更高版本
   - MindManager: 2017 或更高版本

2. **功能限制**：
   - 某些高级样式可能在目标软件中显示略有差异
   - 复杂的自定义样式可能需要在目标软件中重新调整

3. **字符编码**：
   - 所有导出文件都使用 UTF-8 编码
   - 确保目标软件支持 UTF-8 编码以正确显示中文内容
