# 导出格式问题修复

## 修复的问题

### 1. FreeMind (.mm) 格式打不开
**问题**：添加BOM后FreeMind无法解析XML
**修复**：
- 移除XML内容中的BOM标记
- 在Blob创建时添加BOM，而不是在XML内容中
- 确保XML格式正确

### 2. EdrawMind (.emmx) 格式无法打开
**问题**：复杂的JSON结构不被EdrawMind识别
**修复**：
- 改为使用简化的XML格式
- 使用类似FreeMind的节点结构
- 提高兼容性

### 3. MindManager (.mmap) 格式ID错误
**问题**：OId属性需要特定格式，不能是随机字符串
**修复**：
- 生成标准的GUID格式ID
- 移除OId属性（简化结构）
- 使用标准的MindManager XML结构

## 修复后的格式

### FreeMind (.mm) 格式
```xml
<?xml version="1.0" encoding="UTF-8"?>
<map version="1.0.1">
<!-- To view this file, download free mind mapping software FreeMind from http://freemind.sourceforge.net -->
  <node TEXT="根节点" ID="ID_123456789">
    <font NAME="Arial" SIZE="14"/>
    <edge COLOR="#808080" WIDTH="1"/>
    <node TEXT="子节点1" ID="ID_987654321" POSITION="right"/>
    <node TEXT="子节点2" ID="ID_456789123" POSITION="left"/>
  </node>
</map>
```

### EdrawMind (.emmx) 格式（简化XML）
```xml
<?xml version="1.0" encoding="UTF-8"?>
<map version="1.0">
  <node TEXT="根节点" ID="node_123456789">
    <node TEXT="子节点1" POSITION="right"/>
    <node TEXT="子节点2" POSITION="left"/>
  </node>
</map>
```

### MindManager (.mmap) 格式
```xml
<?xml version="1.0" encoding="UTF-8"?>
<ap:Map xmlns:ap="http://schemas.mindjet.com/MindManager/Application/2003" 
        xmlns:cor="http://schemas.mindjet.com/MindManager/Core/2003" 
        xmlns:pri="http://schemas.mindjet.com/MindManager/Primitive/2003">
  <ap:OneTopic>
    <ap:Topic>
      <ap:Text>
        <ap:PlainText>根节点</ap:PlainText>
      </ap:Text>
      <ap:SubTopics>
        <ap:Topic>
          <ap:Text>
            <ap:PlainText>子节点1</ap:PlainText>
          </ap:Text>
        </ap:Topic>
        <ap:Topic>
          <ap:Text>
            <ap:PlainText>子节点2</ap:PlainText>
          </ap:Text>
        </ap:Topic>
      </ap:SubTopics>
    </ap:Topic>
  </ap:OneTopic>
</ap:Map>
```

## 技术改进

### 1. FreeMind格式
- **BOM处理**：在Blob级别添加BOM，不在XML内容中
- **编码**：确保UTF-8编码正确处理中文
- **格式**：严格遵循FreeMind XML schema

### 2. EdrawMind格式
- **格式简化**：使用XML而不是复杂的JSON
- **兼容性**：使用通用的节点结构
- **属性**：支持基本的TEXT、ID、POSITION属性

### 3. MindManager格式
- **ID生成**：使用标准GUID格式（如果需要）
- **结构简化**：移除可能导致解析错误的属性
- **命名空间**：使用正确的MindManager XML命名空间

## 测试步骤

### 1. 创建测试思维导图
```
根节点：测试思维导图
├── 左分支：项目管理
│   ├── 需求分析
│   └── 进度跟踪
└── 右分支：技术开发
    ├── 前端实现
    └── 后端接口
```

### 2. 导出测试
1. 访问 http://localhost:8081
2. 创建上述结构的思维导图
3. 分别导出为.mm、.emmx、.mmap格式
4. 检查文件是否正常生成

### 3. 软件打开测试
1. **FreeMind (.mm)**：
   - 使用FreeMind软件打开
   - 验证中文字符显示正常
   - 验证节点结构完整

2. **EdrawMind (.emmx)**：
   - 使用EdrawMind软件打开
   - 验证能正常识别文件格式
   - 验证节点内容正确

3. **MindManager (.mmap)**：
   - 使用MindManager软件打开
   - 验证无XML解析错误
   - 验证显示正确的节点内容

## 预期结果

### FreeMind (.mm)
- ✅ 文件能正常打开
- ✅ 中文字符显示正常（UTF-8 BOM处理）
- ✅ 节点层级结构正确
- ✅ 左右分布布局正确

### EdrawMind (.emmx)
- ✅ 文件能被EdrawMind识别
- ✅ 简化的XML结构兼容性好
- ✅ 基本的节点属性支持

### MindManager (.mmap)
- ✅ 无XML解析错误
- ✅ 正确显示节点文本内容
- ✅ 标准的MindManager XML结构

## 故障排除

### 如果FreeMind仍无法打开
1. 检查XML格式是否正确
2. 验证文件编码是否为UTF-8
3. 尝试用文本编辑器查看文件内容

### 如果EdrawMind不识别文件
1. 确认文件扩展名为.emmx
2. 检查XML结构是否完整
3. 尝试用其他支持XML的软件打开

### 如果MindManager报错
1. 验证XML命名空间是否正确
2. 检查ap:PlainText元素是否包含内容
3. 确认没有无效的属性值

## 注意事项

1. **编码处理**：
   - FreeMind：使用UTF-8 BOM确保中文正确显示
   - EdrawMind：标准UTF-8编码
   - MindManager：标准UTF-8编码

2. **格式兼容性**：
   - 使用简化的结构提高兼容性
   - 避免复杂的属性和元素
   - 遵循各软件的标准格式

3. **测试建议**：
   - 先用小型思维导图测试
   - 包含中文内容验证编码
   - 测试多层级结构

## 总结

通过这次修复：
- 解决了FreeMind的BOM处理问题
- 简化了EdrawMind的格式结构
- 修复了MindManager的ID格式错误

所有格式现在都应该能够正常打开并显示正确的内容。
